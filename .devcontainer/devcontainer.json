// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/docker-outside-of-docker
{
	"name": "mac-dev-playbook",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	"image": "devcontainer-ansible:3.13-slim-bookworm",
	"mounts": [
    	"source=/Users/andyg/.ssh,target=/.ssh,type=bind,consistency=cached",
		// "source=/Users/andyg/.zsh_history,target=/root/.zsh_history,type=bind,consistency=cached"
    ]
	// "runArgs": ["--network=host"]
}
